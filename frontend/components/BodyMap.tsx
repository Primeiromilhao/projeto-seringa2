'use client';
type Site={id:string;name:string;quadrant:string;status:'recommended'|'resting'|'blocked'};
export default function BodyMap({sites,onSelect}:{sites:Site[];onSelect:(id:string)=>void}){return <div className="rounded-3xl bg-white p-5 shadow-sm"><h2 className="font-semibold">Rodízio de locais</h2><div className="mx-auto my-5 grid max-w-sm grid-cols-2 gap-3">{sites.map(s=><button key={s.id} onClick={()=>s.status!=='blocked'&&onSelect(s.id)} className={`rounded-2xl border p-4 text-left ${s.status==='recommended'?'border-emerald-400 bg-emerald-50':s.status==='resting'?'border-amber-400 bg-amber-50':'border-red-400 bg-red-50 opacity-70'}`}><div className="font-medium">{s.name}</div><div className="text-xs mt-1">{s.status==='recommended'?'Recomendado hoje':s.status==='resting'?'Em repouso':'Bloqueado <48h'}</div></button>)}</div><div className="flex justify-center gap-4 text-xs"><span>🟢 recomendado</span><span>🟡 repouso</span><span>🔴 bloqueado</span></div></div>}
