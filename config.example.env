# Copy to .env or set in the process environment.
OPENAI_API_KEY=

# Model is configured in code for now; no secret is stored in this project.
# The key must never be committed, logged, or exposed to another component.
